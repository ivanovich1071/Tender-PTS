# Шаг 1 — сбор и интерфейс
fastapi>=0.115
uvicorn[standard]>=0.30
requests>=2.31
PySocks>=1.7            # socks5-прокси для белорусских площадок

# Шаг 2-3 — документация и чтение чертежей (ставится по мере надобности)
# PyMuPDF>=1.24
# Pillow>=10.0
# opencv-python-headless>=4.10

# Десктопная версия: своё окно вместо браузера.
# Нужен только для `python -m app --window` и для сборки .exe —
# при обычном запуске через localhost не требуется.
pywebview>=6.0

# Сборка .exe. Ставится отдельно, в работе приложения не участвует:
#   .venv/Scripts/python.exe -m pip install pyinstaller
#   .venv/Scripts/python.exe build_desktop.py
# pyinstaller>=6.0
